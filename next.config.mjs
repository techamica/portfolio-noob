/** @type {import('next').NextConfig} */
const nextConfig = {
    basePath: '/portfolio_noob',
    publicRuntimeConfig: {
        basePath: '/portfolio_noob', // Make basePath available globally
    },
};

export default nextConfig;
